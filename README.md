# ocean-ml-closure

The file learning.jl only reads temperature 2 meter datasets.
Using these datasets to predict the furute 2 meter temperature.
The region variable can be changed.

Modle input: 1(temperature) + 4(seasonal)
Output: 1(next month temp)
